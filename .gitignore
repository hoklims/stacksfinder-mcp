# Dependencies
node_modules/

# Build output
dist/

# SvelteKit (shouldn't be here but just in case)
.svelte-kit/

# Lock files (keep package-lock.json for Smithery deployment)
bun.lock
yarn.lock
pnpm-lock.yaml

# Registry tokens (sensitive)
.mcpregistry_*
